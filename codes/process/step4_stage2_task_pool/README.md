# Step 4 Stage-2 Task Pool

本目录对应流程中的 `Step 4`：

- 读取 `Step 3` 的第一阶段粗标结果
- 判断哪些帧或段需要升级到 `Step 5`
- 生成第二阶段任务池

当前 active 仓库里，这一步的生产实现仍未独立落地。

保留这个目录的目的，是让 `Step 0` 到 `Step 5` 的主线结构在代码层显式存在，而不是混在说明文字里。
